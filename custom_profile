# Things we inject into the environment.
# alias debug_print="echo"
function debug_print() { :; }

. ~/.dotfiles/repo/git_aliases
. ~/.dotfiles/repo/more_aliases
. ~/.dotfiles/repo/exports
. ~/.dotfiles/repo/profile.sh

# Functions!
. ~/.dotfiles/repo/functions/e_related.sh

# Let's start putting anything shell-agnostic in here, instead of in *rc

# Sounds
set bell-style none


# This must be the last thing in this file.
# Otherwise, it cannot overwrite things (e.g. exports from ~/.dotfiles/repo/exports)
if [ -f ~/.dotfiles/local_exports ]; then 
    . ~/.dotfiles/local_exports
fi
if [ -f ~/.dotfiles/local_profile ]; then 
    . ~/.dotfiles/local_profile
fi
### LAST LINE OF FILE ###
